import { JwtLoginView } from '@/sections/auth';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'IOT',
};

export default function LoginPage() {
  return <JwtLoginView />;
}
